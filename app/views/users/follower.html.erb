<h1><%= @user.name %> Followers</h1>

<% if @followers.empty? %>
    <p class="none">You do not have any followers yet.</p>
<% else %>
    <% @followers.each do |user| %>
        <div class="user"><%= link_to user.name, user_url(user.id) %></div>
    <% end %>
<% end %>